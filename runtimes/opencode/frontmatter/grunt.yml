description: {{DESCRIPTION}}
mode: subagent
model: {{MODEL}}
temperature: {{TEMPERATURE}}
permission:
  edit: allow
  task: deny
  external_directory: deny
  bash:
    "*": ask
    "git status*": allow
    "git diff*": allow
    "npm run lint*": allow
    "npm test*": allow
    "dotnet build*": allow
    "dotnet test*": allow
    "git commit*": deny
    "git push*": deny
